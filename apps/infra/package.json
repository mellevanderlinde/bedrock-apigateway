{
  "name": "infra",
  "version": "0.1.0",
  "bin": {
    "bedrock-apigateway": "bin/bedrock-apigateway.js"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:update": "vitest run -u",
    "lint": "eslint ."
  },
  "devDependencies": {
    "aws-cdk": "^2.174.1",
    "esbuild": "^0.24.2",
    "eslint-config": "workspace:*",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.174.1",
    "bedrock": "workspace:*",
    "constructs": "^10.4.2"
  }
}
