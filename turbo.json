{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "outputs": ["src/**/*.js", "src/**/*.d.ts"]
    },
    "test": {
      "dependsOn": ["^test", "build"]
    },
    "test:update": {
      "dependsOn": ["^test", "build"]
    }
  }
}
